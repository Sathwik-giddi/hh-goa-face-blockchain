# $0 Mac-native stack — no paid APIs, no GPU, no dlib build hell
opencv-python==4.10.0.84
numpy>=1.24,<3
requests>=2.31,<3
python-dotenv>=1.0
Pillow>=10.0
tqdm>=4.66

# EVM (Polygon Amoy) — free faucet
web3>=6.11,<7
eth-account>=0.10
py-solc-x>=2.0  # deploy-time only: compiles contracts/FaceAnchor.sol

# Frontend (10/10 UI)
fastapi>=0.110,<1
uvicorn>=0.29
python-multipart>=0.0.9

# Atomic chain writes (filelock + tempfile)
filelock>=3.12
