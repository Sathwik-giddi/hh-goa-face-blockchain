{
  "version": 1,
  "ports": {"preview": 3000, "additional": {"app": 3000}},
  "scripts": {
    "setup": {"enabled": true, "command": "apt-get update -qq && apt-get install -y -qq python3-dev && python3 -m venv .venv && .venv/bin/python -m pip install -r requirements.txt"},
    "run": {"enabled": true, "command": ".venv/bin/python -m uvicorn app:app --host 0.0.0.0 --port 3000 --reload"},
    "archive": {"enabled": true, "command": null},
    "check": {"enabled": true, "command": null}
  },
  "mcpServers": []
}
